CN_estimate <- function (dat.cytogenetics, gene.list, gene.infos)
{
        CN.df <- data.frame(matrix(0,nrow=nrow(dat.cytogenetics), ncol=length(gene.list)))

        # plus
        # minus
        # add
        # del
        
}
